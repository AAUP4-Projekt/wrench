use logos::Logos;
//use super::token::print_tokens;

pub fn create_ast(input: &str) {
    //let lexer = Logos::lexer(input);
    //let tokens = Token::collect_tokens(lexer);
    //print_tokens(&tokens);
}